DOMAIN=control.example.com
VAYUVEER_TOKEN=replace-with-a-long-random-secret

# Dashboard accounts live in deploy/users.json. Create one with:
#   python3 ../server/manage_users.py add <user> <operator|viewer> <drone-id,...|*>
